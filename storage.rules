rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 認証済みのユーザーに対して、すべてのファイルの読み取りと書き込みを許可します。
      // これにより、プログラムのアップロードと表示が両方とも正しく機能するようになります。
      allow read, write: if request.auth != null;
    }
  }
}
