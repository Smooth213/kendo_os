# Instructions

- 常に日本語で回答してください。
- git commit や git push などの Git 操作は指示がない限りエージェント側で自動実行しないでください（コミット・プッシュはユーザー自身が手動で行います）。
- 修正の最後には必ずコード分析とデプロイを実行してください。
- `./scripts/deploy_web.sh` を実行する前に必ず `flutter analyze` を実行し、警告・エラーが0件の状態であることを確認してください。

### Flutter アプリの修正・デプロイ手順

1. **コード修正**
   - 依頼内容に応じた実装・修正を行います。

2. **コード分析（必須）**
   ```bash
   flutter analyze