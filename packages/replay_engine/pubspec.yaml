name: replay_engine
description: "Isolated deterministic state replay engine for kendo_os."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.9.0
  kendo_os:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
